{% extends 'base.twig' %}
{% from 'form.twig' import input, textarea, select %}

{% block content  %}
  <div class="block-title">
      <h2>Liste des utilisateurs</h2>
  </div>

  <div class="row">
      <div class="col s12 m8 l12">
        <table id="data-table-simple" class="table table-striped table-bordered table-vcenter table-hover table-condensed">
          <thead>
            <tr>
              <th>Nom</th>
              <th>Pseudo</th>
              <th>Mail</th>
              <th>Numéro</th>
              <th></th>
            </tr>
          </thead>
          <tbody>
            {% for key, utilisateur in utilisateurs %}   
            <tr>
              <td> {{ utilisateur.nom }} </td>
              <td> {{ utilisateur.login }} </td>
              <td> {{ utilisateur.email }} </td>
              <td> {{ utilisateur.numero }} </td>
              <td> 
                <div class="col s12 m8 l9">
                  <a class="btn dropdown-button waves-effect waves-light cyan" href="javascript:void(0);" data-activates="dropdown{{ utilisateur.id }}">Options<i class="mdi-navigation-arrow-drop-down right"></i></a>
                  <ul id="dropdown{{ utilisateur.id }}" class="dropdown-content">
                    <li><a href="{{ path_for('utilisateur.show')~'?id='~utilisateur.id }}" class="-text">Modifier</a>
                    </li>
                    <li><a href="#!" class="-text">Supprimer</a>
                    </li>
                  </ul>
                </div>
              </td>
            </tr>
            {% endfor %}
          </tbody>
        </table>
      </div>
  </div>
{% endblock %}